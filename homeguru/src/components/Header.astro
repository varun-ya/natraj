---
---

<header class="header">
  <div class="container">
    <nav class="nav">
      <a href="/" class="logo">HomeGuru</a>
      <div class="nav-links">
        <button class="nav-item">Platform</button>
        <button class="nav-item">Developers</button>
        <button class="nav-item">Blogs</button>
        <button class="nav-item">Company</button>
      </div>
      <div class="nav-actions">
        <button class="btn btn-primary">Experience HomeGuru</button>
        <button class="btn btn-secondary">Login</button>
      </div>
    </nav>
  </div>
</header>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid #f0f0f0;
  }

  .nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1rem 0;
  }

  .logo {
    font-size: 1.5rem;
    font-weight: 600;
    text-decoration: none;
    color: #1a1a1a;
  }

  .nav-links {
    display: flex;
    gap: 1rem;
  }

  .nav-item {
    background: none;
    border: none;
    padding: 0.5rem 1rem;
    font-size: 0.875rem;
    font-weight: 500;
    text-transform: uppercase;
    cursor: pointer;
    color: #1a1a1a;
    letter-spacing: 1px;
  }

  .nav-actions {
    display: flex;
    gap: 0.75rem;
  }

  .btn {
    padding: 0.75rem 1.5rem;
    border-radius: 9999px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s;
    border: none;
  }

  .btn-primary {
    background: #131313;
    color: white;
    box-shadow: inset 0 0 12px rgba(255,255,255,1);
  }

  .btn-secondary {
    background: #fafafa;
    color: #1a1a1a;
    box-shadow: inset 0 0 12px rgba(0,0,0,0.09);
  }

  @media (max-width: 768px) {
    .nav-links, .nav-actions {
      display: none;
    }
  }
</style>
